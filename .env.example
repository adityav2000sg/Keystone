# Supabase (web — public)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase (API — JWT verify / admin)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# API base URL (for OAuth redirects; e.g. https://api.staging.keystone.example.com)
API_URL=

# Google OAuth (API only)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
